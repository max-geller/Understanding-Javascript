// Arrays are useful for logs, using brackets. Arrays start with index of 'zero':
let logEntry = [
    "hello world",
    42,
    true
]

console.log(logEntry[1]); // 42

// Use ARRAYS for numerically positioned values, and OBJECTS for named properties